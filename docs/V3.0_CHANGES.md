# V3.0 변경 내역 (vs 12.5-front-end)

## 1) 제거된 건설현장 흔적

| 항목 | 처리 |
| --- | --- |
| `/polygon` 라우트 | 완전 삭제 |
| 사이드바 "위험구역 설정" 메뉴 | "AI 진단"으로 교체 |
| TopHeader "생활 구역" 타이틀 | "AI 진단"으로 교체 |
| `ZONE_INTRUSION` 이벤트 타입 | `eventLabels.ts`, `mock-events.json`에서 제거 |
| "현장명/현장별/주의 현장/정상 운영 현장" 어휘 | "병원·센터명/병원·센터별/..."로 교체 |
| `PolygonPage`, `PolygonEditor`, `polygonApi`, `usePolygonEditor`, `types/polygon` | **완전 삭제** |

## 2) V2.0 PoC에서 가져온 반려동물 진단 기능

`/diagnose` 페이지 — 환부 사진 업로드 + 안구/피부 라디오 + Top-3 확률 + 수의사 권고 가이드 + JSON 결과 + 샘플 이미지로 빠른 테스트.

| 신규 파일 | 역할 |
| --- | --- |
| `src/pages/DiagnosePage.tsx` | 진단 페이지 메인 |
| `src/components/diagnose/*.tsx` | 6개 컴포넌트 (ImageUpload, RegionRadio, PredictionResult, ActionPlanCard, JsonResultPanel, SampleGallery) |
| `src/hooks/useDiagnose.ts` | 진단 훅 (progress 0→100% 시뮬, 실모델 자리) |
| `src/lib/diagnoseLogic.ts` | EYE/SKIN_CLASSES, ACTION_PLAN_DB, mockPredict() |
| `src/types/diagnose.ts` | 진단 결과 타입 |
| `public/samples/{eye,skin}_placeholder.png` | 샘플 이미지 |

## 3) 회원 시스템 + 마이페이지 (NEW)

### 인증 시스템 (mock, localStorage 기반)

| 항목 | 상세 |
| --- | --- |
| 회원가입 (`/signup`) | 이메일/비밀번호/이름 → localStorage에 저장, 자동 로그인 후 `/me`로 이동 |
| 로그인 (`/login`) | 저장된 계정과 비교, 세션 발급. 테스트 계정 5종 원클릭 채우기 버튼 |
| 로그아웃 | 사이드바 + 마이페이지에서 가능 |
| 라우트 가드 | `/me`는 비로그인 시 `/login`으로 리다이렉트 |
| Sidebar / TopHeader | 로그인 상태 표시(이름 + 로그아웃) ↔ 미로그인(로그인/회원가입) 동적 전환 |

### 임의 회원 5명 seed (앱 첫 실행 시 자동 주입)

비밀번호는 모두 `pw1234`.

| 이메일 | 이름 | 등록 반려동물 |
| --- | --- | --- |
| `mango@petcare.kr` | 김민지 | 망고 (푸들, 4살) |
| `junpark@petcare.kr` | 박서준 | 나비 (코리안숏헤어, 5살) |
| `sualee@petcare.kr` | 이수아 | 보리 (포메라니안), 콩이 (시츄) |
| `doyoun@petcare.kr` | 최도윤 | 초코 (시바견) |
| `haneul@petcare.kr` | 정하늘 | 미카 (러시안블루), 라떼 (먼치킨) |

### 마이페이지 (`/me`)

- **내 정보 카드**: 이름·전화번호·단골 병원 인라인 수정, 이메일/가입일은 읽기 전용, 로그아웃 버튼
- **내 반려동물 카드 그리드**: 사진/이름/종/품종/나이/몸무게/특이사항 표시, 등록·수정·삭제 모달
- **PetEditModal**: 사진 URL 또는 파일 업로드(dataURL 변환), 종 라디오(강아지/고양이/기타), 출생연도·몸무게·특이사항

### 신규 파일

| 파일 | 역할 |
| --- | --- |
| `src/types/auth.ts` | User, Session, PublicUser |
| `src/types/pet.ts` | Pet, PetSpecies |
| `src/lib/authStorage.ts` | localStorage 기반 사용자 DB + 5명 seed + 펫 seed + 세션 |
| `src/hooks/useAuth.ts` | login/signup/logout/updateProfile/currentUser |
| `src/hooks/usePetProfile.ts` | 현재 로그인 유저 기준 펫 CRUD |
| `src/components/auth/RequireAuth.tsx` | 로그인 가드 |
| `src/components/me/OwnerCard.tsx` | 보호자 정보 카드 |
| `src/components/me/PetCard.tsx` | 반려동물 카드 |
| `src/components/me/PetEditModal.tsx` | 등록·수정 모달 |
| `src/pages/MyPage.tsx` | 마이페이지 (lazy 라우트) |

## 4) 어휘/타입 정리

- `types/event.ts`에 `PetEventType` 별도 정의 — `DISEASE_ALERT | BEHAVIOR_ANOMALY` 만
- 모든 "현장" 표현 → "병원·센터" 통일

## 5) 고아 코드 / 미사용 자산 삭제

- `src/components/dashboard/{DashboardCharts,SiteSummaryCards}.tsx` (orphan)
- `src/lib/dashboardStats.ts` (위 두 파일 전용)
- `src/assets/{react.svg,vite.svg,hero.png}` (Vite 템플릿 잔재)
- → `dashboard/`, `assets/` 폴더 함께 제거

## 6) 그대로 남긴 것

- `apiBase.ts`의 `VITE_API_BASE_URL` 환경변수 패턴 — mock 폴백 동작 유지
- 리포트 PDF 다운로드 흐름 (`/reports`)
- recharts, lucide-react, Tailwind v4

## 7) 보안 주의사항 (중요)

> ⚠ 본 데모는 사용자 비밀번호와 세션을 **브라우저 localStorage에 평문으로 저장**합니다.
> 실서비스에서는 절대 사용 금지. 백엔드 인증 API(JWT/세션 쿠키)로 교체 필요.
> 교체 위치: `src/hooks/useAuth.ts` + `src/lib/authStorage.ts`
